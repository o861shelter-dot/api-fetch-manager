# [FEATURE/FIX] Batch 08 — Inspect + Fetch Builder + UI chung — ĐÃ HOÀN THÀNH

> Tổng theo dõi task 08. Base: PR #5 (nền tảng) → PR #7 (JSX/backend, đã merge) → PR này (ráp Fetch Builder).
> Tài liệu: `docs/SPEC-PLAN/02.SPEC_UI-addendum-v1.2.md`. Rule: `AGENTS.md`.
> ⚠️ PR #6 là bản LÀM TRÙNG của task này (conflict do đụng file với #7) → nên ĐÓNG, không resolve.

---

## A. Nền tảng cross-cutting (PR #5) — ✅ DONE
- [x] Tooltip auto-flip/clamp; rule `.kv-row`; guard 401 Admin token; fix Credentials trùng row+card; SPEC_UI addendum v1.2.

## B. Inspect Mode (PR #7) — ✅ DONE
- [x] Hotkey Ctrl+Shift+J (env override) mọi form/modal; toolbar Tạo issue/Tạm ngưng/Thoát; ESC thoát; nhả con trỏ; chạy trên modal; element có text; panel #/selector/text; IssueElement.text + markdown.

## C. Backend (PR #7) — ✅ DONE
- [x] catalogs dùng chung; flow-presets lưu DB + seed 4 mẫu; credential-keys; test.

## D. Component dùng chung (PR #7) — ✅ DONE
- [x] Combobox (options + onSaveOption); KeyPicker (chèn {{key}} + advanced JS + resolved).

## E. Ráp Fetch Builder UI (PR NÀY) — ✅ DONE
- [x] Tên flow/Service/Business → Combobox (options từ `/api/catalogs`, onSaveOption lưu danh mục).
- [x] URL/header/body → KeyPicker chèn `{{key}}`; preset header Authorization/Content-Type/Accept.
- [x] Body: chọn định dạng JSON/raw + nút Beautify (bỏ qua khi có placeholder).
- [x] Tạo từ mẫu (GET `/flow-presets`) + Lưu thành mẫu (POST `/flow-presets`).

## G. InputsEditor — ✅ DONE (giữ nguyên từ bản gốc, đã có trong builder)
- [x] Khai báo inputs runtime/store/context trong BuilderModal.

---

## ⏳ CÒN LẠI — chỉ verify + nâng cấp tuỳ chọn

### F. Verify runtime (BẮT BUỘC trước merge) — ✅ DONE
- [x] `npm install && npm test` → xanh (catalogs-presets, credential-import, api). Repo hiện chạy ở root, không có thư mục `app`.
- [x] `npm run build` → tsc + vite sạch. `FetchBuilderPage` import `Combobox`, `KeyPicker`, `ExecuteModal` khớp signature.
- [x] `npm run seed` → pass; UI "Tạo từ mẫu" hiện đủ 4 mẫu mặc định. Local DB đang có thêm 1 preset copy nên tổng hiện 5.
- [x] Smoke 3 breakpoint: app render, không console error, hotkey inspect, token modal cân đối, Fetch Builder controls/preset/Beautify có mặt. Đã fix overflow mobile trong Builder modal.

### H. Field forwardRef (tuỳ chọn, P2) — ⏳
- [ ] Hiện KeyPicker append vào cuối field (không cần ref) → chạy được. Nếu muốn chèn tại caret: thêm forwardRef cho Input/Textarea (đã có ở PR #6, có thể cherry-pick).

### I. Placeholder autocomplete khi gõ `{{` ([UI] 10.6) — ⏳ P2
- [ ] Dropdown gợi ý `{{ctx/var/input/credential}}` + transform. Hiện dùng KeyPicker + preset thay thế.

### K. Docs — ✅ DONE
- [x] Đánh dấu addendum v1.2 §8 hoàn thành; ghi nhật ký `docs/SPEC-PLAN/04.PROMPT.md`.
- [x] Đồng bộ tên key `cloudflare.token` giữa preset và credential thực tế/docs.

---

## Ghi chú xử lý PR #6 (conflict)
PR #6 và #7 làm trùng cùng task 08 trên nhánh nền tảng. #7 đã merge (ở commit trước phần Fetch Builder wiring). PR này bổ sung đúng phần Fetch Builder còn thiếu, dùng lại Combobox/KeyPicker đã merge → KHÔNG đụng lại 10 file của #7. **Hành động: đóng #6 (superseded), merge PR này.**
