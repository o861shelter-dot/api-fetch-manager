# [FEATURE/FIX] Phần phụ thuộc JSX (làm tiếp sau PR nền tảng)

> Nền tảng cross-cutting đã xong ở PR `feat/cross-cutting-ui-foundation` (tooltip.ts, components.css, api.ts guard, docs addendum v1.2). Task này là phần cần sửa file JSX (App/InspectMode/FetchBuilder) + backend catalog/presets.
> Đọc: `docs/SPEC-PLAN/02.SPEC_UI-addendum-v1.2.md`. Template: `.templates/change-request/functions/inspect-mode.*` + `fetch-builder.*`.

## A. Inspect Mode (`frontend/src/features/inspect/InspectMode.tsx`, `App.tsx`)
- [ ] Hotkey toàn cục Ctrl+Shift+J (env `VITE_API_FETCH_MANAGER_INSPECT_HOTKEY`) bật inspect ở mọi form/modal.
- [ ] Thanh công cụ: nút Tạo issue (n) + **Tạm ngưng** + **Thoát**; **ESC = thoát** (dùng `.inspect-toolbar`).
- [ ] Tạm ngưng / mở form issue → body thêm class `inspect-paused` (nhả con trỏ).
- [ ] Inspect chạy trên modal (bỏ loại trừ `.overlay`, chỉ loại trừ `.inspect-toolbar`).
- [ ] Mỗi element lưu thêm `text` (innerText cắt ~200 ký tự). Panel dùng `.sel-item` (#, selector, text).
- BE: `backend/src/lib/types.ts` IssueElement thêm `text?`; `lib/markdown.ts` in kèm text.

## B. Modal Admin token (`App.tsx`)
- [ ] Dùng `.modal--narrow` + `.field--center`, label trên input.

## C. Fetch Builder (`frontend/src/pages/FetchBuilderPage.tsx`)
- [ ] `Combobox` cho Tên flow/Service/Business + lưu danh mục (BE `/api/catalogs`).
- [ ] `KeyPicker` chèn `{{key}}` + preset header + xem resolved (`/api/engine/resolve`).
- [ ] Body: chọn định dạng + nút Beautify.
- [ ] Tạo từ mẫu / Lưu thành mẫu; seed 4 preset trong DB.

## D. Backend mới
- [ ] `catalogs` (node rtdb-keys/catalogs): `GET/POST /api/catalogs?field=` — danh mục dùng chung.
- [ ] `flow-presets` (node rtdb-keys/flow-presets, cờ isPreset): `GET /api/flow-presets`, seed 4 mẫu mặc định trong `scripts/seed-smoke.ts` hoặc seed riêng.
- [ ] Cập nhật `docker/database.rules.json` `.indexOn` nếu cần.

## Test
- [ ] `backend/test/*`: catalogs/flow-presets CRUD; IssueElement.text trong markdown.
- [ ] `npm test` + `npm run build` xanh (local, không CI).
