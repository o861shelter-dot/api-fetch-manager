# [FEATURE/FIX] Phần phụ thuộc JSX (làm tiếp sau PR nền tảng) — ĐÃ XONG

> Nền tảng cross-cutting ở PR `feat/cross-cutting-ui-foundation` (tooltip.ts, components.css, api.ts guard, docs addendum v1.2). Task này = phần JSX (App/InspectMode/FetchBuilder) + backend catalog/presets, làm ở `feat/inspect-fetchbuilder-jsx`.
> Đọc: `docs/SPEC-PLAN/02.SPEC_UI-addendum-v1.2.md`.

## A. Inspect Mode (`InspectMode.tsx`, `App.tsx`) — ✅
- [x] Hotkey toàn cục Ctrl+Shift+J (env `VITE_API_FETCH_MANAGER_INSPECT_HOTKEY`) bật inspect ở mọi form/modal.
- [x] Thanh công cụ: Tạo issue (n) + **Tạm ngưng** + **Thoát**; **ESC = thoát** (`.inspect-toolbar`).
- [x] Tạm ngưng / mở form issue → body thêm class `inspect-paused` (nhả con trỏ).
- [x] Inspect chạy trên modal (chỉ loại trừ `.inspect-toolbar`/tooltip, overlay nằm dưới z-inspect).
- [x] Mỗi element lưu thêm `text` (innerText ~200 ký tự). Panel `.sel-item` (#, selector, text).
- [x] BE: `types.ts` IssueElement.text; `markdown.ts` in kèm text.

## B. Modal Admin token (`App.tsx`) — ✅
- [x] `.field--center`, label trên input, canh giữa.

## C. Fetch Builder (`FetchBuilderPage.tsx`) — ✅
- [x] `Combobox` cho Tên flow/Service/Business + lưu danh mục (`/api/catalogs`).
- [x] `KeyPicker` chèn `{{key}}` + preset header (Auth/JSON) + xem resolved (`/api/engine/resolve`).
- [x] Body: chọn định dạng (JSON/raw/form) + nút Beautify.
- [x] Tạo từ mẫu (PresetPicker) / Lưu thành mẫu; seed 4 preset trong DB.

## D. Backend mới — ✅
- [x] `catalogs`: `GET/POST /api/catalogs?field=` (dùng chung).
- [x] `flow-presets`: `GET/POST /api/flow-presets` (isPreset), seed 4 mẫu trong `scripts/seed-smoke.ts`.
- [x] `GET /api/owners/:id/credential-keys` cho KeyPicker.
- [x] Field forwardRef (Input/Textarea/Select) để chèn placeholder vào body.

## Test / verify (cần chạy local, không CI)
- [ ] `npm test` + `npm run build` xanh.
- [ ] `npm run seed` → có 4 flow preset; UI "Tạo từ mẫu" hiện đủ.
- [ ] Hotkey Ctrl+Shift+J bật inspect trên 1 modal; ESC thoát; tạm ngưng nhả con trỏ.
- [ ] Body beautify + chèn key hoạt động; combobox lưu/chọn danh mục.
