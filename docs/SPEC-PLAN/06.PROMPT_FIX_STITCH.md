# 06. PROMPT FIX — Áp giao diện Stitch + sửa nghiệp vụ (dựa trên HIỆN TRẠNG repo)

> **Gửi cho agent để LÀM LẠI đúng.** Khung đã build (76 test pass) nhưng **CHƯA áp đúng thiết kế Stitch + chưa đúng nghiệp vụ owner-centric**. Chỉ tập trung khoảng cách còn lại.
>
> **Bản ClickUp:** page `03.PROMPT_FIX_STITCH`.

---

## A0. ĐỌC TRƯỚC
1. `AGENTS.md` + `docs/SPEC-PLAN/02.SPEC_UI.md` + addendum v1.2→v1.6.
2. **Thiết kế gốc (bắt buộc):** `stitch_prompt_execution_system/*/code.html` (HTML+Tailwind thật) + `screen.png`. Lấy layout/màu/spacing/typography từ đây, KHÔNG tự chế.
3. `npm install && npm run dev`, so UI hiện tại vs `screen.png`.

## A1. NGUYÊN TẮC
- So 1-1 với Stitch từng màn, chỉnh cho khớp layout/màu/spacing/typography/trạng thái.
- Không phá logic BE đã đúng; chỉ sửa FE + bổ sung endpoint thiếu. Giữ golden rules [UI] §0.

---

## B. DANH SÁCH SỬA

### B1. [P0] Trang OWNERS — ĐANG THIẾU
Nav đang bắt đầu từ Credentials; Stitch (`api_fetch_manager_layout_frame/code.html`) có **System Owners đầy đủ**.
- Thêm **OwnersPage** + nav "Owners" đầu sidebar.
- `DataList`: Email · isSaveRtdbEmail · Created At · Services (badge) + filter (Status/Created At/Creds Count) + Export CSV + Refresh.
- 4 action/dòng (icon+tooltip): Toggle Active · Create Fetch · View Credentials · Go to Service.
- CRUD owner qua modal; xóa confirm.
- **DoD:** khớp `screen.png`; 4 action chạy; đổi active owner → mọi trang cập nhật.

### B2. [P0] Áp TOKENS Stitch
Stitch: bg `#0e1510`, surface `#1a211c`, primary `#60eca8`, border `#2a2f37`, JetBrains Mono cho code. Đồng bộ `tokens.css` theo `tailwind.config` trong `code.html`. Row 32px, sidebar 220px thu gọn, topbar 48px, status bar đúng.
- **DoD:** so `screen.png` 3 màn, sai khác không đáng kể.

### B3. [P1] credId — key trùng resolve đúng
KeyPicker liệt kê từng credId (label + masked + credId), chèn ref theo credId. Resolve ưu tiên credId; fallback key chỉ khi 1 giá trị; nhiều giá trị chưa chọn → báo lỗi.
- **DoD:** 2 `github.token` / 4 `supabase.database` → chọn A/B → resolve đúng.

### B4. [P1] Services/Resources — refresh + item-level
- `POST /api/resources/refresh` → chạy `listTemplateId` → lưu snapshot; nút Refresh đổ dữ liệu.
- Item-level: "Tạo Fetch Builder từ item" + "Lấy var" + cron "Gọi xóa theo id" (confirm).

### B5. [P2] Extracted Data CRUD
Thêm POST/PUT/DELETE `/api/extractions` + Sửa/Xóa (confirm) + Pin biến trên UI.

### B6. [P2] Docs 5 dịch vụ
Hoàn thiện cloudflare/dpdns/tailscale/supabase/cron-job.md theo mẫu `github.md`.

---

## C. QUY TRÌNH
Mỗi mục: đọc `code.html` Stitch → sửa → `npm test`+`npm run build` xanh → nhật ký `04.PROMPT.md`. PR nhỏ B1→B6.

## D. NGHIỆM THU
- [ ] OwnersPage đầy đủ + 4 action, khớp Stitch.
- [ ] Tokens đồng bộ Stitch (3 màn so screen.png).
- [ ] credId key trùng resolve đúng + case Self-Test.
- [ ] resources/refresh + item-level (xóa cron theo id).
- [ ] Extracted Data CRUD.
- [ ] 6 docs dịch vụ.
- [ ] `npm test` ≥ 76 xanh, build xanh, Self-Test case fetch pass.

> ⚠️ Token `00.YEUCAU.md` §6 đã lộ → rotate; seed/test dữ liệu GIẢ.
